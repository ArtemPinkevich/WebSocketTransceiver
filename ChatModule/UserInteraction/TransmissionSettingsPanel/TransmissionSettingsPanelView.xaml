<UserControl x:Class="ChatModule.UserInteraction.TransmissionSettingsPanel.TransmissionSettingsPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mvvm:ViewModelLocator.AutoWireViewModel="True"
             xmlns:local="clr-namespace:ChatModule.UserInteraction.TransmissionSettingsPanel"
             xmlns:mvvm="http://prismlibrary.com/"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance local:TransmissionSettingsPanelViewModel}">
    <Grid>
        <CheckBox Content="Repeater Mode" IsChecked="{Binding Path=IsRepeaterModeEnabled}" Command="{Binding Path=SwitchRepeaterModeCommand}"/>
    </Grid>
</UserControl>
